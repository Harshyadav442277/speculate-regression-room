# Claude worker — demonstration review and handoff

Codex is the integrator. Read REVIEW.md, DEMO.md, JUDGE_VERDICT.md and the latest CLAUDE_STATUS.md first. Acknowledge this assignment in CLAUDE_STATUS.md before editing. Preserve all existing work.

Own only CLAUDE_DEMO_REVIEW.md and your status append. The runtime has passed 35 integrated tests; freeze feature work. Do not add agents, tests, providers, adapters, dependencies, integrations or architecture. No git, deployment, credentials, external messages or product-model calls in this assignment.

Write a short review that answers these concrete questions:

1. Can a judge follow the path from wrong quote to saved test, and run the test themselves? Walk the current documented commands and inspect the exported script's assumptions. Report any actual missing step. Do not repeat tests already green unless a new concern justifies one.
2. Give a 90-second narration focused on the user's result. One real peer-driven change supports the story; the exported regression file is the payoff. Mark every line that cannot be spoken until a real provider run exists.
3. Propose one fair evaluation case using the existing fixture/input controls that could challenge the preferred explanation. It is a public development case, not a blind benchmark. Explain what evidence would falsify the diagnosis. No invented win/saving numbers.
4. List at most three remaining obstacles, with the cheapest concrete next action for each. Do not call unsigned local records tamper-evident or claim universal root cause. Do not estimate ranking probabilities without evidence.

Do not bypass Codex's rejected Vercel deployment command. Automatic approval review failed because of its usage limit; the user has been given the manual command. Product model key and private hosted code are also missing. Real-run evaluation waits for the user to configure them.

Return a concise status with the file path. Codex owns merging any demo changes and deciding the next live-run task.
