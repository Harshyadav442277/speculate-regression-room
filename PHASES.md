# SPECULATE — phases and protected schedule

Updated September 6. Decision fixed September 5, approximately 22:35 IST. The schedule below is a plan; the current evidence is separated here.

## Current progress

- Built: pinned dependencies, installed Mozaik runtime, three policies, executable evidence, exports, local UI/API and Vercel streaming handlers.
- Checked: controlled runtime and HTTP tests, local browser journey and corrected-case execution. Final counts and hosted checks are in VERIFICATION.md.
- Blocked on model access: first real investigation, natural peer-driven action change, comparative evaluation and a real saved demonstration.
- Checked independently of credentials: fresh local Git checkout with frozen cached install, saved-run viewer with the application server stopped after load, changed inputs, and phone-sized layout. The latest source checkpoint will be rechecked before handoff.
- Still required: full dead-network rehearsal with a genuine recording, video, judge access to source, dashboard confirmation and submission.

Claude Opus 5 completed its runtime tasks and CLAUDE_NEXT_TASK.md demonstration review. Codex incorporated the concrete findings and owns final integration, source backup and submission preparation. The user is handling the supplied deployment command, provider access and participant-dashboard details.

## Schedule authority

The [official rules](https://build.jigjoy.ai/rules), read earlier in this session, state September 7 at 09:00 CET and also 03:00 ET / midnight PT. Those labels differ by one hour under September daylight-time assumptions. Plan against 12:30 IST, the earlier interpretation, and confirm any dashboard/organizer clarification. The latest direct web refetch failed; no new schedule change was verified.

Internal feature freeze: **September 6, 19:30 IST**. Internal submission target: **September 7, 06:00 IST**. Both are deliberate buffers, not official requirements. Move the freeze earlier if available working hours shrink.

## Phase 0 — finalization and feasibility evidence

Completed: independent idea passes; reciprocal review; selection of the bounded SPECULATE concept; isolated upstream control-flow check; executable checkout fixture with twelve HTTP cases and three invalid-input checks. The six required project documents now exist.

Real model calls, useful live cooperation, comparative evaluation, recording and submission remain incomplete. Installation and UI are now implemented; see the current progress above for later phases.

## Phase 1 — live runtime vertical slice (first 60 minutes of building)

Use a provider configured through the local project environment; do not copy credentials from unrelated projects. Pin/install Mozaik, validate declarations, run two actual agents, and capture overlap plus a peer-triggered proposal revision against the fixture.

Exit evidence: reproducible command; actual provider-backed trace; an earlier action proposal and its changed successor; checked API results. The source mock is not this milestone. Stop configuration churn at the timebox and report the exact failure. Reduce implementation complexity within the chosen concept instead of launching a new brainstorming round.

## Phase 2 — complete useful path (approximately 3–4 focused hours)

Integrate immutable evidence, bounded replanning, conclusion/report generation, run reset, and the same probe set against the labelled corrected fixture. Keep the agent context free of fixture answers. Add meaningful checks for run isolation, evidence provenance, action revisions, and failure preservation.

## Phase 3 — prove the contribution (approximately 1–2 focused hours)

Run the same harness with replanning on/off and a capable adaptive control. Reserve an evaluator-only configuration before tuning. Capture correctness, time, actual probes, model requests/usage, and failures. Do not resample only until the preferred mode wins.

An observed benefit can be a timely corrected action with defensible evidence; a speed/cost superiority claim requires the corresponding measured result. If the agents provide no useful contribution, narrow the claimed benefit and review the task design promptly.

## Phase 4 — one screen and judge path (approximately 3–4 focused hours)

Implement the two lanes, evidence/result view, report export, and visible live/recorded mode. Use the appropriate site-building skills when implementation starts. Run the full journey in a browser. Update `DEMO.md` with actual application commands and exact tested steps.

## Phase 5 — reliability before freeze (approximately 2–3 focused hours)

Exercise missing credentials, provider timeout, malformed probe request, one agent failure, run-budget exhaustion, reset, and evidence reuse. Verify a clean checkout/install and the no-network fallback separately. Record a real successful provider-backed run for the fallback artifact. Fix issues; add no unrelated functionality.

## Phase 6 — freeze, rehearse, record, submit

After the freeze, only fixes, usability corrections, packaging, verification, video, and submission. Rehearse the 90–120 second target, verify all displayed numbers against recorded events, ensure the repository is accessible to judges, and complete the actual submission fields. A local run is not a submission.

## Work allocation and ownership

Planning allocation: concurrency/cooperation 35%; technical execution 25%; creativity/evaluation 15%; demo/UX 15%; user/report usefulness 10%. These are internal build-hour weights, not published judging weights. Mozaik participation is an eligibility requirement; substantial usage supports the concurrency and execution case.

Active split: Claude—runtime, agents, action-boundary correction; Codex—fixture, evaluation, verification, UI, hosting and integration review. Claude has acknowledged and implemented its assignment. Read shared documents before starting, claim modules before editing, and preserve peer work. Only Codex edits dependency manifests and shared event types.

If behind: cut the second UI mode toggle, decorative trace visualization, extra probes, and export styling before cutting real-provider validation, source attribution, failure handling, or the complete judge journey. Do not add a third agent to solve a coordination bug.
